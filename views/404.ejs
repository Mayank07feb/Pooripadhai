<!-- 404 Hero Section -->
<section class="relative bg-gray-50 text-gray-900 overflow-hidden font-montserrat min-h-screen flex items-center py-16">
    <div class="max-w-4xl mx-auto px-6 text-center relative z-10">

        <!-- Optional Illustration / Image -->
        <div class="mb-8">
            <img src="https://stories.freepiklabs.com/storage/23103/404-error-rafiki-2773.png" alt="404 Illustration"
                class="mx-auto w-2/3 md:w-1/2 lg:w-1/3 object-contain animate-fadeIn">
        </div>

        <!-- Main Heading -->
        <h1 class="text-6xl md:text-7xl font-extrabold text-primary mb-4">404</h1>

        <!-- Subheading -->
        <h2 class="text-2xl md:text-3xl font-bold mb-4 text-secondary">
            Oops! Page Not Found
        </h2>

        <!-- Description -->
        <p class="text-base md:text-lg text-gray-700 mb-8 leading-relaxed">
            The page you are looking for does not exist. It might have been removed, had its name changed, or is
            temporarily unavailable.
        </p>

        <!-- Button to go home -->
        <a href="/"
            class="inline-block px-10 py-4 bg-primary text-white font-semibold rounded-xl shadow-lg hover:bg-primary-dark hover:scale-105 transition-all duration-300">
            Go to Home →
        </a>

    </div>
</section>

<!-- Tailwind CSS Animation (optional) -->
<style>
    @keyframes fadeIn {
        0% {
            opacity: 0;
            transform: translateY(-20px);
        }

        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fadeIn {
        animation: fadeIn 1s ease-in-out;
    }
</style>